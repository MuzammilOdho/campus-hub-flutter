Removed legacy/demo files during refactor (per user confirmation):

- archive/ (entire directory) — legacy/archived code and unused modules
- lib/src/screens/campushub_ui_demo.dart — demo screen (replaced by `campus_hub_screen.dart`)
- test/campushub_demo_widget_test.dart — legacy demo widget test (replaced by focused unit tests)

If you need any of these removed files restored, they're available in your VCS history; let me know and I can restore specific files instead of the whole archive.

